fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
psycopg[binary]>=3.2.10
geoalchemy2==0.15.2
python-multipart==0.0.9
websockets==13.0
pydantic-settings==2.5.2
python-dotenv==1.0.1
aiofiles==24.1.0
# Stage 2 — AI Predictive Disruption Engine
scikit-learn>=1.5.0
numpy>=1.26.0
# v4 only: the code calls cell_to_latlng / latlng_to_cell / cell_to_boundary,
# which were introduced in 4.0.0 (v3 named these h3_to_geo / geo_to_h3 /
# h3_to_geo_boundary). The previous h3>=3.7.7 permitted a 3.x resolution that
# fails at import. Upper bound guards against the next API-breaking major.
h3>=4.0,<5
httpx>=0.27.0
apscheduler>=3.10.4
# Stage 3 — Dynamic Routing & Fleet Optimization
networkx>=3.3
shapely>=2.0
# Stage 4 — Alert Dispatch & Cloud Integration
boto3>=1.34
