# Copy to fleet-manager/.env if the backend is not on localhost:8000.
# In dev the Vite proxy forwards /api to the backend on the same origin, so this
# is only needed for a build served from somewhere else.
# VITE_API_URL=http://localhost:8000
